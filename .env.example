# Copy to .env in repo root (Docker Compose loads it automatically).
# ADMIN_API_KEY must be the raw string printed once by:
#   docker compose exec backend python scripts/create_api_key.py --label "admin-feedback"
ADMIN_API_KEY=
